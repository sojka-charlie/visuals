{
  "name": "visuals-portfolio",
  "version": "1.0.0",
  "description": "Portfolio of interactions and UI tools - Apple ML Research Figures Capture",
  "scripts": {
    "capture": "node capture-figures.js",
    "install-browsers": "npx playwright install chromium",
    "setup": "npm install && npm run install-browsers"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^24.9.1",
    "playwright": "^1.56.1"
  }
}
